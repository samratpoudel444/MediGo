

export const deletePharmacy = async(req, res, next)=>
{
    try{
        const {id}= req.params.id;
        const checkIfUserExist = PharmacyTable
    }
    catch(err)
    {
        console.log(err);

    }
}